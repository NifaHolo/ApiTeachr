{% extends 'base.html.twig' %}

{% block title %}Teachr{% endblock %}

{% block body %}
    <h1>Teachr</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ teachr.id }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ teachr.prenom }}</td>
            </tr>
            <tr>
                <th>Created_at</th>
                <td>{{ teachr.createdAt ? teachr.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('teachr_index') }}">back to list</a>

    <a href="{{ path('teachr_edit', {'id': teachr.id}) }}">edit</a>

    {{ include('teachr/_delete_form.html.twig') }}
{% endblock %}
